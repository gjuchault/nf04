#include <iostream>
#include <cstring>
#include <cmath>

using namespace std;

bool NON (bool assert)
{
    return !assert;
}

template<typename T>
T real_abs(T value) {
    if (value < 0) {
        return -value;
    }
    return value;
}

structssousalgo
int main ()
{
    globalVariables
